import type {Video} from '@/lib/videos';
import {durationLabel,videoHref} from '@/lib/videos';
import {timelineHref} from '@/lib/posts';
export default function VideoCard({video:v,feed=false}:{video:Video;feed?:boolean}){return <div className={'video-card '+(feed?'video-in-feed':'')}><a className="video-cover" href={videoHref(v.id)} aria-label={'观看：'+v.title}><img src={v.thumbnail} alt={v.title+' · 示意封面'} loading="lazy"/><span className="video-cover-label">{v.category}</span><span className="video-play">▶</span><span className="video-duration">{durationLabel(v.duration)}</span></a><div className="video-card-meta"><span>视频 · {v.category}</span><span>{v.isMemberOnly?'会员内容':'公开'}</span></div><h3><a href={videoHref(v.id)}>{v.title}</a></h3>{feed&&<p>{v.description}</p>}<div className="video-card-foot">{v.symbol?<a href={timelineHref(v.symbol,v.market)}>{v.symbol} · 完整研究档案 ↗</a>:<span>趋势交易课 · {Number(v.id.split('-').at(-1))} 课</span>}<span>{v.publishedAt.slice(0,10)}</span></div></div>}
