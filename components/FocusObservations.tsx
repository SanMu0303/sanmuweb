
import type {WatchItem} from '@/lib/types';

export default function FocusObservations({items}:{items:WatchItem[]}){const active=[...items].filter(w=>w.stage!=='失效'&&w.observationStatus!=='ended'&&!w.endedAt).sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt)).slice(0,3);return <section className="focus-section"><div className="section-line"><h2>当前重点观察 <span>{String(active.length).padStart(2,'0')}</span></h2><a href="/watchlist/">全部观察 ↗</a></div><div className="focus-grid">{active.map(w=><a className="focus-observation" key={w.id||w.symbol} href={'/watchlist/'+encodeURIComponent(w.id||w.symbol)+'/'}><div><b>{w.symbol}</b><span className={'record-stage record-stage-'+w.stage}>{w.stage}</span></div><p>{w.thesis}</p><footer><time dateTime={w.updatedAt}>{w.updatedAt.slice(5)}</time></footer></a>)}</div>{!active.length&&<p className="muted">暂无正在跟踪的重点标的。</p>}</section>}
