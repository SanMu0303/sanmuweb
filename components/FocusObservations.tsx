
import type {WatchItem} from '@/lib/types';
import {timelineHref} from '@/lib/posts';
export default function FocusObservations({items}:{items:WatchItem[]}){const active=[...items].filter(w=>w.stage!=='失效').sort((a,b)=>b.updatedAt.localeCompare(a.updatedAt)).slice(0,3);return <section className="focus-section"><div className="section-line"><h2>当前重点观察 <span>{String(active.length).padStart(2,'0')}</span></h2><a href="/watchlist/">全部观察 ↗</a></div><div className="focus-grid">{active.map(w=><a className="focus-observation" key={w.symbol} href={timelineHref(w.symbol,w.market.replace('A 股','A股').replace('加密市场','加密').replace('全球市场','黄金'))}><div><b>{w.symbol}</b><span className={'record-stage record-stage-'+w.stage}>{w.stage}</span></div><h3>{w.name}<small>{w.market}</small></h3><p>{w.thesis}</p><footer><span>{w.updatedAt.slice(5)}</span><span>研究时间线 ↗</span></footer></a>)}</div>{!active.length&&<p className="muted">暂无正在跟踪的重点标的。</p>}</section>}
